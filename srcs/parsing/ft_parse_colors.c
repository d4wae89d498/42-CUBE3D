#include "cube3d.h"

t_bool  ft_parse_colors(int fd, char *line)
{
    size_t  index;

    index = 0;
    while (index < 2)
    {
        // IMPLEMENT HERE RETRIEVE IF ITS FLOOR OR CEILING, CREATE A STRUCT FOR RGB COLOR AND FILL IT PROPRELY WITH "," AS SEPARATOR.
    }
}